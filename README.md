# 🔑 Генератор ключей и паролей

Универсальный инструмент для генерации серийных ключей, логинов и паролей с графическим интерфейсом.

## 🚀 Возможности

### Генератор ключей:
- Случайная генерация по длине
- Генерация по шаблонам (XXXX-YYYY-ZZZZ)
- Анализ образцов для создания похожих ключей
- Настройка типов символов (цифры, буквы, символы)

### Генератор паролей:
- Умная генерация читаемых логинов
- Сложные пароли с настройкой сложности
- Создание связок логин:пароль
- Генерация email адресов (логин@домен)

## 📦 Установка

1. Установите Python 3.10+
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt

 # 🔑 Key and password generator

Universal tool for generating serial keys, logins and passwords with a graphical interface.

## 🚀 Features

### Key generator:
- Random generation by length
- Generation by templates (XXXX-YYYY-ZZZZ)
- Sample analysis to create similar keys
- Custom character types (numbers, letters, symbols)

### Password generator:
- Smart generation of readable logins
- Complex passwords with complexity settings
- Creation of login:password combinations
- Generation of email addresses (login@domain)

## 📦 Installation

1. Install Python 3.10+
2. Install dependencies:
```bash
pip install -r requirements.txt  